<% for (var i = 0; i < games.length; i ++) { %>
	<div class="_img imgNormal" style='background-image: url("http://cdn.akamai.steamstatic.com/steam/apps/<%= games[i]['appId'] %>/header.jpg")' data-appId=<%= games[i]['appId'] %>>
		<% if (games[i]['priceOverview']['discount_percent'] > 0) { %>
		<div class="discountPercent _discountPercent">
			<span>-<%= games[i]['priceOverview']['discount_percent'] %>%</span>
		</div>
		<% } %>
		<div class="gamesDetail _gamesDetail hide">
			<div class="bottom">
				<span class="name"><%= games[i]['name'] %></span>
				<span class="hours"><span class="fa fa-clock-o clock"></span> <%= games[i]['playTimeForever'] %>h</span>
				<% if (games[i]['totalAchievements'] > 0) { %>
					<span class="achievements"><span class="fa fa-flag flag"></span> <%= games[i]['totalAchievementsCompleted'] %>/<%= games[i]['totalAchievements'] %></span>
				<% } %>
				<% if (games[i]['priceOverview']['final'] > 0) { %>
					<span class="pricing right">
						<% if (games[i]['priceOverview']['discount_percent'] > 0) { %>
						<span class="cost discount"><%= games[i]['priceOverview']['initial'] %></span>
						<% } %>
						<span class="cost right">$<%= games[i]['priceOverview']['final'] %></span>
					</span>
				<% } else { %>
					<span class="pricing right">
						<span class="cost right">FREE</span>
					</span>
				<% } %>
			</div>
		</div>
	</div>
<% } %>
